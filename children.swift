      root.children.addAll(moveLeftButton, moveRightButton, playerRectangle)
        val scene = Scene(root, 800.0, 600.0)
        primaryStage.title = "Kotlin Scratch Game"
        primaryStage.scene = scene
        primaryStage.show()
    }
